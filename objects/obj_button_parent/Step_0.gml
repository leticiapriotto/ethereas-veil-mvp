/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor

// Step event
if (position_meeting(mouse_x, mouse_y, id)) {
    image_index = 1;  // Change to the second frame when mouse is over
} else {
    image_index = 0;  // Change back to the first frame when mouse is not over
}
